overwrite: true
schema: "./src/schema/schema.ts"
documents: null
generates:
  src/@types/graphql.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
